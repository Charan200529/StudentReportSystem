FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci || npm install

COPY . .

# Match frontend code which reads VITE_API_URL
ENV VITE_API_URL=http://localhost:8081/api

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]


